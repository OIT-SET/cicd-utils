name: Publish Release
on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version number to publish, ie. 1.0.0'
        required: true

jobs:
  create-release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Create Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          VERSION=${{ github.event.inputs.version }}
          echo "Creating release for version v$VERSION"
          gh release create "v$VERSION" --title "v$VERSION" --generate-notes --notes "Release notes for version $VERSION:"
      - name: Upload Release Assets
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          VERSION=${{ github.event.inputs.version }}
          echo "Uploading assets for version v$VERSION"
          gh release upload "v$VERSION" ./scripts/builder
          gh release upload "v$VERSION" ./scripts/install-dependencies
          gh release upload "v$VERSION" ./scripts/oracle-setup
